
#include <stdio.h>

int main()
{   int n;
    printf(" Enter the number :");
    scanf("%d",&n);
    int c=0; int a=n;
    while(n!= 0)
    {
        n=n/10;
        c++;
        
    }
    printf("The number %d has %d digits", a,c);

    return 0;
}
